import { render, screen } from '@testing-library/react';
import ExchangeSearch from './ExchangeSearch';

describe('ExchangeSearch Component', () => {
  xit('test template', () => {
    render(<ExchangeSearch />);
  });
});
